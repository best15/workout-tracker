const { route } = require("./api");

const router = require("express").Router();

// const { Blog, User, Comment } = require("../models");

// router.get("/", (req, res) => {
//     db.Book.find({})
//       .then(dbBook => {
//         res.json(dbBook);
//       })
//       .catch(err => {
//         res.json(err);
//       });
//   });